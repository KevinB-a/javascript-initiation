/* la boucle for doit etre codée de cette facon
    for ([expressionInitiale]; [condition]; [expressionIncrément]) 
    {
        instruction 
    }
  
*/

// exemple de boucle for
for (let pas = 0; pas < 5; pas++) 
{
    // Ceci sera exécuté 5 fois
    // À chaque éxécution, la variable "pas" augmentera de 1
    // Lorsque'elle sera arrivée à 5, le boucle se terminera.
    console.log('Faire ' + pas + ' pas vers l\'est'); // l'anti slash sert a placer l'apostrophe
}

/* la boucle while doit etre codee de cette facon 
    while (condition)
    {
        instruction 
    }
        
*/